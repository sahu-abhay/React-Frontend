here we use useCallback hook which is store cache function and add a dependencies only that dependancies re-render than function re-create other-wise not

and useEffect run a function where any dependencies has changed or affeted